import 'package:flutter/material.dart';

class FiturB extends StatelessWidget {
  const FiturB({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Abghi'),
    );
  }
}
